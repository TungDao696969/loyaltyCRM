model Admin {
  id           Int      @id @default(autoincrement())
  username     String   @unique
  password     String
  fullName     String
  role         Role
  refreshToken String?
  createdAt    DateTime @default(now())
}

enum Role {
  ADMIN
  SUPER_ADMIN
}
